module.exports = {
    printWidth: 80,               // Wrap lines at 80 characters
    tabWidth: 2,                  // Use 2 spaces per tab
    useTabs: false,               // Indent with spaces instead of tabs
    semi: true,                   // Add semicolons at the ends of statements
    singleQuote: true,            // Use single quotes instead of double quotes
    jsxSingleQuote: false,        // Use double quotes in JSX
    trailingComma: 'es5',         // Add trailing commas where valid in ES5 (objects, arrays, etc.)
    bracketSpacing: true,         // Print spaces between brackets in object literals
    jsxBracketSameLine: false,    // Put the `>` of a multi-line JSX element at the end of the last line
    arrowParens: 'avoid',         // Omit parens when arrow functions have a single argument
    endOfLine: 'auto',            // Maintain existing line endings (useful for mixed projects)
    proseWrap: 'preserve',        // Don't wrap prose in Markdown files
};